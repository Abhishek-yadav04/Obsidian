Place your project logo image files here.

Filename to use by UI: logo.png

Tip: Save the provided attachment (your logo) as `logo.png` and place it in this folder. The UI references `/ui/assets/logo.png`.

If you want a different filename or path, update references in `ui/index.html` and `ui/login.html` accordingly.
